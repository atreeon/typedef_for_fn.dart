name: example

environment:  
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  typedef_for_fn:
    path: ../typedef_for_fn

dev_dependencies:
  typedef_for_fn_generator:
    path: ../typedef_for_fn_generator/
  build_runner: ^2.1.4
  test: ^1.24.2